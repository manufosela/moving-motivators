{
  "rules": {
    ".read": "auth.provider === 'google' && auth.token.email.matches(/.*@kairosds.com$/)",
    ".write": "auth.provider === 'google' && auth.token.email.matches(/.*@kairosds.com$/)"
  }
}